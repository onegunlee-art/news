# -*- coding: utf-8 -*-
"""Generate 특허 명세 초안 Word — 개인화 텍스트 의사결정(판단) 학습 방법 및 시스템."""
from pathlib import Path

from docx import Document
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.shared import Cm, Pt, RGBColor


def add_heading(doc: Document, text: str, level: int = 1) -> None:
    p = doc.add_paragraph()
    r = p.add_run(text)
    r.bold = True
    if level == 1:
        r.font.size = Pt(12)
    else:
        r.font.size = Pt(11)
    p.paragraph_format.space_before = Pt(12)
    p.paragraph_format.space_after = Pt(6)


def add_body(doc: Document, text: str) -> None:
    for block in text.strip().split("\n\n"):
        doc.add_paragraph(block.strip())


def main() -> None:
    root = Path(__file__).resolve().parents[1]
    out_path = root / "docs" / "patent_personalized_judgment_engine_draft.docx"
    out_path.parent.mkdir(parents=True, exist_ok=True)

    doc = Document()
    section = doc.sections[0]
    section.top_margin = Cm(2)
    section.bottom_margin = Cm(2)
    section.left_margin = Cm(2.5)
    section.right_margin = Cm(2.5)

    title = doc.add_paragraph()
    title.alignment = WD_ALIGN_PARAGRAPH.CENTER
    tr = title.add_run(
        "특허 명세 초안 — 인공지능 초안·인간 수정 상호작용 및 수정 근거 잠재표현 분석을 이용한 "
        "개인화된 텍스트 의사결정 모델 학습 방법 및 시스템"
    )
    tr.bold = True
    tr.font.size = Pt(13)
    title.paragraph_format.space_after = Pt(14)

    subtitle = doc.add_paragraph()
    subtitle.alignment = WD_ALIGN_PARAGRAPH.CENTER
    sr = subtitle.add_run("(변리사 검토 전 참고용 초안)")
    sr.italic = True
    sr.font.size = Pt(10)
    subtitle.paragraph_format.space_after = Pt(18)

    add_heading(doc, "【발명의 명칭】")
    add_body(
        doc,
        "인공지능 초안–인간 수정 상호작용 및 수정 이유 잠재 분석을 이용한 "
        "상황 맥락 조건부 개인화 텍스트 의사결정 학습 방법 및 시스템",
    )

    add_heading(doc, "【기술분야】")
    add_body(
        doc,
        """본 발명은 자연어 처리 및 인간–기계 협업 편집에 관한 것으로, 특히 언어 모델이 생성한 초안에 대한 인간의 수정 행위와 그에 대한 잠재(latent) 차원의 근거 표현을 학습하여, 단순 표현 스타일이 아닌 상황·맥락 조건 하에서의 개인별 편집·선택 판단을 재현하는 방법 및 시스템에 관한다.""",
    )

    add_heading(doc, "【배경기술】")
    add_body(
        doc,
        """통상의 스타일 전이(stylistic transfer) 또는 화자 일관성 모델은 어휘·문장 길이·톤 등 표층적 특징에 치우치기 쉽고, “왜 그 문장을 그렇게 고쳤는지”에 해당하는 편집 의도·우선순위·거부 기준을 명시적으로 학습하지 않는다.

반면 실제 편집은 동일한 표현이라도 독자·매체·법적 리스크·브랜드 기준 등 상황 맥락에 따라 다른 선택을 유발한다. 따라서 개인화된 글쓰기 지원을 위해서는 표면 스타일 모방을 넘어, 수정 전후 차이와 수정 이유를 잠재 공간에서 해석·정렬하고, 맥락 조건부로 최종 선택을 예측하는 판단 구조가 필요하다.""",
    )

    add_heading(doc, "【발명이 해결하고자 하는 과제】")
    add_body(
        doc,
        """본 발명이 해결하고자 하는 과제는 다음과 같다.

첫째, AI 초안 대비 인간 수정을 단순 문자열 차이가 아닌 의사결정 단위로 구조화할 것.

둘째, 수정 이유를 명시 라벨에만 의존하지 않고 잠재 표현으로 추출·정렬하여 확장 가능한 감독 신호로 쓸 것.

셋째, 상황·맥락을 조건으로 두어 동일 개인이라도 조건별로 다른 판단 분포를 출력할 것.

넷째, 초안 생성 → 인간 수정 → 수정 근거 잠재 분석 → 상황 맥락 부착 → 최종 선택 학습의 반복 루프로 단순 스타일링 모델이 아닌 개인화된 판단 엔진을 정밀화할 것.""",
    )

    add_heading(doc, "【과제를 해결하기 위한 수단】")
    add_body(
        doc,
        """일 실시예에 따른 본 발명은 다음 단계를 포함한다.

(1) 초안 생성 단계: 하나 이상의 언어 모델 또는 편집 모델로 과업 맥락에 맞는 초안 텍스트를 생성한다.

(2) 인간 수정 수집 단계: 사용자 인터페이스를 통해 초안에 대한 삭제·삽입·치환·재배열 등 편집 궤적과, 선택적으로 짧은 코멘트·태그 등 명시적 근거를 수집한다.

(3) 수정 전후 정렬 단계: 초안과 수정본 간의 정렬(alignment)을 통해 후보 편집 연산 단위로 분해한다.

(4) 수정 이유 잠재 분석 단계: 수정 전후 쌍, 선택된 편집 연산, 선택적 사용자 코멘트를 입력으로 하여 저차원 잠재 벡터 또는 잠재 구조를 학습한다. 동일한 표면 수정이라도 서로 다른 잠재 근거를 구분 가능하도록 대조 학습 목표를 둘 수 있다.

(5) 상황·맥락 인코딩 단계: 과업 유형, 독자층, 채널, 규제 플래그, 시간 제약, 참조 문서 요약 등을 맥락 텐서로 인코딩한다.

(6) 최종 선택 학습 단계: 맥락 텐서와 잠재 근거 표현을 조건으로, 인간이 실제로 채택한 수정본 또는 편집 연산 시퀀스에 대한 확률을 최대화하는 조건부 정책 또는 보상 모델을 학습한다.

(7) 루프 갱신 단계: 새로운 초안–수정 데이터가 제공될 때마다 상기 단계를 반하여 개인별 또는 그룹별 판단 분포를 갱신한다.

본 발명에서 말하는 “스타일링”과 구별되는 점은, 출력이 주로 어휘·문법 통계를 모방하는 것이 아니라 주어진 맥락에서 어떤 편집·삭제·유지를 선택할 확률이 높은지를 예측하고, 그 선택을 잠재 근거와 연결하여 해석 가능한 구조로 유지하는 데 있다.""",
    )

    add_heading(doc, "【발명의 효과】")
    add_body(
        doc,
        """표면 스타일 모방을 넘어 편집 의사결정 분포를 학습할 수 있다.

명시 라벨이 부족한 환경에서도 수정 행위 자체로부터 근거 잠재축을 확보할 수 있다.

맥락 조건부 학습으로 동일 사용자 내에서도 과업별 일관된 판단을 분리·재현할 수 있다.

지속적 상호작용 루프를 통해 개인화된 판단 엔진을 데이터 축적으로 정교화할 수 있다.""",
    )

    add_heading(doc, "【도면의 간단한 설명】")
    add_body(
        doc,
        """도 1: AI 초안 생성, 인간 편집, 정렬·편집 연산 추출, 수정 이유 잠재 분석, 맥락 인코딩, 최종 선택 학습, 모델 갱신을 포함하는 순환 흐름도.

도 2: 수정 전후 텍스트와 편집 연산이 잠재 공간으로 사영되는 블록도.

도 3: 맥락 텐서와 잠재 근거가 결합되어 채택 확률을 산출하는 구성도.""",
    )

    add_heading(doc, "【청구항】")
    add_body(
        doc,
        """【청구항 1】(방법)
컴퓨터에 의해 실행되는 텍스트 의사결정 학습 방법으로서,
(a) 언어 모델로 초안 텍스트를 생성하는 단계;
(b) 사용자 인터페이스를 통해 상기 초안에 대한 편집 궤적으로 수정본 텍스트를 수집하는 단계;
(c) 상기 초안과 수정본 간의 정렬에 기초하여 하나 이상의 편집 연산을 산출하는 단계;
(d) 상기 초안, 수정본, 편집 연산, 및 선택적으로 사용자가 제공한 코멘트로부터 수정 근거를 나타내는 잠재 표현을 학습 또는 산출하는 단계;
(e) 과업 또는 환경 정보를 나타내는 맥락 특징을 인코딩하는 단계;
(f) 상기 맥락 특징 및 상기 잠재 표현을 입력으로 하여, 사용자가 채택한 수정본 또는 편집 연산 시퀀스에 대한 확률을 높이도록 모델 파라미터를 갱신하는 단계; 및
(g) 상기 (a) 내지 (f)를 반복하여 개인별 또는 그룹별 의사결정 분포를 갱신하는 단계;
를 포함하는 텍스트 의사결정 학습 방법.

【청구항 2】
청구항 1에 있어서, 상기 (d) 단계는 동일한 표면적 편집이라도 상이한 수정 근거를 구분하기 위해 대조 학습 목표를 포함하는 것을 특징으로 하는 방법.

【청구항 3】
청구항 1에 있어서, 상기 (f) 단계는 보상 모델 또는 역강화학습 정책 중 적어도 하나를 포함하는 것을 특징으로 하는 방법.

【청구항 4】
청구항 1 내지 3 중 어느 한 항에 있어서, 상기 잠재 표현은 편집 연산의 순서 및 종류와 연관된 인과 구조를 근사하는 것을 특징으로 하는 방법.

【청구항 5】(시스템)
청구항 1의 각 단계를 수행하도록 구성된 하나 이상의 프로세서 및 메모리를 포함하는 텍스트 의사결정 학습 시스템.

【청구항 6】(저장매체)
청구항 1의 방법을 컴퓨터가 실행하도록 하는 프로그램이 저장된 비일시적 컴퓨터 판독 가능 저장매체.""",
    )

    add_heading(doc, "【발명을 실시하기 위한 구체적인 내용】")
    add_body(
        doc,
        """일 실시예에서 편집 연산은 Myers diff, 토큰 단위 정렬, 문장 또는 구 단위 재배열 식별 중 적어도 하나에 의해 산출될 수 있다.

일 실시예에서 잠재 분석은 수정 전후를 각각 인코딩한 뒤 차이 벡터를 저차원으로 투영하는 방식, 또는 대조 학습에 의한 프로젝션 방식을 포함할 수 있으며, 사용자 코멘트가 존재하는 경우 멀티모달 결합이 가능하다.

일 실시예에서 맥락 특징은 JSON 스키마 등으로 정규화된 과업 유형, 금지어 목록, 독자 속성, 채널 메타데이터를 포함할 수 있다.

일 실시예에서 학습 목표는 채택 수정본에 대한 교차 엔트로피 최소화와, 거부된 후보에 대한 대조 손실을 결합할 수 있다.

일 실시예에서 개인 데이터 보호를 위해 온디바이스 학습, 차등 프라이버시 노이즈, 사용자별 어댑터 파라미터 분리 중 적어도 하나를 적용할 수 있다.

명세서 작성 시 “범용 인공지능(AGI)” 등 과장·모호 표현은 피하고, 조건부 편집 선택 예측, 수정 근거 잠재 표현, 인간–기계 협업 학습 루프 등 기술적으로 정의 가능한 용어로 기재하는 것이 바람직하다. 미국 등 관할에 따라 순수 알고리즘 청구가 제한될 수 있으므로, 구체적 데이터 구조, 사용자 인터페이스, 학습 단계, 하드웨어 실행과 연계한 실시예를 명세에 포함하는 것이 유리하다.""",
    )

    doc.add_paragraph()
    foot = doc.add_paragraph()
    fr = foot.add_run(
        "본 문서는 출원 전 변리사·지식재산 담당 검토, 선행기술 조사 및 청구항 범위 조정이 필요한 참고 초안입니다."
    )
    fr.italic = True
    fr.font.size = Pt(9)
    fr.font.color.rgb = RGBColor(0x55, 0x55, 0x55)

    doc.save(out_path)

    alt = root / "docs" / "특허_개인화_판단엔진_명세_초안.docx"
    try:
        import shutil

        shutil.copy2(out_path, alt)
    except OSError:
        pass
    print(out_path)


if __name__ == "__main__":
    main()
